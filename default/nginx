upstream {upstream} {
    server unix:/tmp/{domain}.sock;
}

server {
    listen      80;
    server_name {domain};
    charset     utf-8;

    client_max_body_size 5M;

    location / {
        uwsgi_pass  {upstream};
        include     {path}/uwsgi_params;
    }
}
